import AddCustomerForm from '@/app/(customers)/(components)/(forms)/AddCustomerForm';

const page = () => {
  return (
    <div className="space-y-8">
      <h1>Add New Customer</h1>
      <AddCustomerForm />
    </div>
  );
};

export default page;
