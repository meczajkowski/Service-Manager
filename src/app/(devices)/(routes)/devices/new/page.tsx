import DeviceForm from '@/app/(devices)/(components)/DeviceForm';

const page = () => {
  return (
    <div className="space-y-8">
      <h1>Add New Device</h1>
      <DeviceForm />
    </div>
  );
};

export default page;
